# Google OAuth Client ID (required)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# API URL - leave empty when using Docker (Vite proxy handles it)
# Set to http://localhost:8000 when running frontend locally without Docker
VITE_API_URL=
